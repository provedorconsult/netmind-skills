# Promotion criteria

Promotion requires confirmed cause, before evidence, authorized remediation,
usable free disk, Docker/Dokploy/application validation, rotation and
existing-container assessment, prevention, rollback, LIVE-001 update, Skill
retrofit, test/acceptance PASS, CI and `git diff --check`. Only the Checker
authorizes integration under the existing Harness; CI is not merge approval.
