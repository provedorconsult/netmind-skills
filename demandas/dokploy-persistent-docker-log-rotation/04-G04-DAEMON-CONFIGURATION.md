# G04 daemon.json

`DISCOVER → BACKUP → READ → PARSE → MERGE → VALIDATE JSON → REVIEW → APPLY →
RESTART → VALIDATE`. Preserve unrelated keys; handle absent, existing, invalid
JSON and existing log options. End at **PRODUCTION AUTHORIZATION REQUIRED**;
do not execute in this demand.
